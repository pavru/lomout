language: java
jdk:
  - oraclejdk8
before_install:
  - mysql -e "CREATE DATABASE IF NOT EXISTS magemediation CHARACTER SET = 'utf8' COLLATE = 'utf8_general_ci';"
  - mysql -e "CREATE DATABASE IF NOT EXISTS magemediation CHARACTER SET = 'utf8' COLLATE = 'utf8_general_ci';"
  - echo "USE mysql;\nUPDATE user SET password=PASSWORD('root') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root
  - sudo service mysql restart
  - sudo netstat -npa | grep mysql
  - mysql -u root -proot magemediation -e "CREATE TABLE testtab ( id INT(11));"
  - mysql -u root -proot magemediation -e "show tables;"
env:
  - SONAR_TOKEN=a6bd7cae688529f47f0ff298cd6f9825836413db
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
